{
	"name": "obsidian-archivist",
	"version": "0.0.1",
	"description": "An obsidian plugin that automatically keeps your vault ordered.",
	"main": "dist/main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.13.5",
		"esbuild": "^0.25.0",
		"obsidian": "latest",
		"typescript": "^5.7.3"
	},
	"dependencies": {
		"builtin-modules": "^4.0.0",
		"tslib": "^2.8.1"
	}
}
